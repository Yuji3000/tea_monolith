<!DOCTYPE html>
<html>
  <style>
    .header {
      text-align: center;
      margin: 0;
      top: 33%;
    }

    .tea-container {
      margin-top: 20px;
    }
    .text-center {
      margin-top: 50px;
      margin-bottom: 110px;
    }
    /* .d-flex {
      margin-left: 10%;
    } */
    
  </style>

  <body>
    
    <i class="bi-house"></i>

    
      <h1 class="text-center"> 

      </h1>
    


    <div class="container-fluid">
      <div class="row row-cols-2 row-cols-md-3">
            <% @teas.each do |tea|%>
              <div class="col mb-4 d-flex flex-direction-column justify-content-center">
                <div class="card border-dark mb-3 shadow-lg bg-white rounded" style="width: 18rem; height: 28rem;">
                  <%= image_tag ("#{tea.image}"), class: "card-img-top", style: "height: auto; width: auto" %>
                  <div class="card-body d-flex align-items-center flex-column ">
                    <h5 class="card-title d-flex align-items-center flex-column " style="font-size: 18px;">
                      <p><%=tea.name%>  |  $<%=tea.price%></p>
                      <p class="card-text d-flex align-items-center flex-column" style="font-size: 15px; margin-bottom: 1px;"><%= tea.description%></p>
                    </h5>
                      <% if current_user.present? == true %>
                        <% if  @cart.include?(tea.stripe_product_id) %>
                          <%= button_to "Remove from cart", remove_from_cart_path(tea.stripe_product_id), method: :delete %>
                        <% else %>
                          <%= button_to "Add to cart", add_to_cart_path(tea.stripe_product_id), method: :post %>
                        <% end %>
                      <% else %>
                          <%= button_to "Sign up", signup_path, method: :get %>
                      <% end %>
                  </div>
                </div>
            </div>
          <% end %>
      </div>
    </div>




  </body>
</html>